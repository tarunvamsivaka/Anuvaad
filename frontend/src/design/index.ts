/**
 * ANUVAAD DESIGN SYSTEM V2 — Root barrel export
 * Import from '@/design' for everything.
 *
 * CSS: Import '@/design/tokens/index.css' separately (handled in globals.css)
 */

// Primitives (semantic surface/typography atoms)
export * from './primitives';

// Components (design-system-level composites)
export * from './components';
